#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

bundle install

# Do any other automated setup that you need to do here
echo "Please add the following to your ~/.zshrc or ~/.bashrc:"
echo "export RUBY_VERSION=\$(ruby -e 'puts RUBY_VERSION')"
